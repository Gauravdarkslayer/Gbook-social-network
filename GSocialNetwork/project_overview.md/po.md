1. Profiles
    -Profile model
    -Relationship model
        -sender
        -receiver
        -status(accept/sent/already      accepted)
        

2. posts
    -post model
        -content
        -optional image
    -comment model
    -like/unlike model

3. allauth(authentication)
